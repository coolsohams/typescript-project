
enum Weekday {
    Sunday = 1,
    Monday = 2,
    Tuesday = 3,
    Wednesday = 4,
    Thursday = 5,
    Friday = 6,
    Saturday = 7
}

console.log(Weekday.Monday)

var dayType: number = Weekday.Wednesday;

if (dayType == Weekday.Sunday) {
    console.log("Hindi Writing at any time.")
}

if (dayType == Weekday.Monday) {
    console.log("15:00 - 16:00 (TV Time)")
}

//__________________________________________________________________________
